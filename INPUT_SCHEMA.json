{
    "title": "Aggregate dataset fields",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "datasetId": {
            "title": "Dataset ID",
            "type": "string",
            "example": "McBNJRgkkYGgghKAG",
            "description": "The dataset id or name where to aggregate data",
            "editor": "textfield"
        },
        "fields": {
            "title": "Fields",
            "type": "array",
            "description": "The list of fields to aggregate data",
            "editor": "stringList"
        },
        "fieldNames": {
            "title": "Return field names only, instead",
            "description": "Return field names only, instead of field values",
            "type": "boolean",
            "editor": "checkbox"
        },
        "split": {
            "title": "Split field",
            "type": "object",
            "example": {
                "myField": "-"
            },
            "description": "Split a field on a delimiter and treat as separated values",
            "editor": "json"
        }
    },
    "required": [
        "datasetId"
    ]
}
